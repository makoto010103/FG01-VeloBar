## v4.1.2 (2026-03-03)
- **バグ修正**: 「RECORD SET」ボタンが存在しない関数を呼び出しており、押しても何も起きなかった問題を修正。

## v4.1.1 (2026-03-03)
- **バグ修正**: セットアップUIの「重量」ピッカーが初期化されず、選択状態（ハイライト）が正しく表示されない問題を修正。

## v4.1.0 (2026-03-02)
- **複数セット管理とレスト（インターバル）タイマーの実装**:
    - [WebApp] セットアップ画面のピッカー要素を拡張し、従来の「種目・重量・回数」に加え「セット数」と「レスト（分数）」を指定できるようUIを大幅アップデート。
    - [WebApp] セット終了時に自動でレストタイマーが起動する機能を実装。
    - [WebApp] レスト時間満了後（または手動SKIP時）に、次セットのラックアップ用「5秒カウントダウン」へシームレスに自動移行するフルオート・ワークアウトループを構築。

## v4.0.0 (2026-03-02)
- **【メジャーアップデート】市販最高峰デバイス同等の「MPV（平均推進速度）」算出アルゴリズムを実装**:
    - [WebApp] レップ評価の主役を「Peak Velocity」から「MPV」へと完全移行。
    - [WebApp] **デュアルフィルタリング**: 推進区間（Peak）の終了判定にはうねりを消す強平滑化（Size=5）を使いつつ、MPVの計算自体には初速の立ち上がり（RFD）を一切ロスしない弱平滑化（Size=2）のピュアな値を使用。
    - [WebApp] **物理法則に基づく算術平均**: サンプリングレート対策として距離重みハック（速度二乗平均）に逃げず、「変位÷時間」というMPVの物理的定義に完全に準拠した算術平均（`Σv / N`）を実装。
    - [WebApp] **絶対値Slackトリミング**: シャフトのしなりや静止摩擦が終わる `0.05m/s` をMPVの開始点（Start Index）として固定。筋発火区間を不当に削らないよう修正。
    - [WebApp] **Neural Readiness**: 画面端に `Peak: X.XX` を残し、セッションベストの最高速度に対する現在ピークの比率を「神経系の活性度（Neural Readiness %）」として表示するコーチングUIを追加。

## v3.8.0-fw (2026-03-02)
- **測定精度の抜本的改善（高級機アルゴリズムの実装）**:
    - [Firmware] ZUPT連動型のオフセット（ゼロ点）自動補正を実装。非挙上時のデバイスの傾斜や固有ノイズを学習・ロックし、純粋な運動加速度のみを抽出するよう修正。
    - [Firmware] 動的リーキー積分を導入。常に0.992を掛けていた指数関数的減衰を廃止し、挙上フェーズ中は `0.9995`（ロスほぼゼロ）、下降時は `0.90` とする動的なクランプを実装。これにより、2秒の粘るスローレップでも実力の81%以上が画面に抽出されるようになり、極めてクリーンな波形を送信可能に。

## v3.7.3 (2026-03-02)
- **Safariのスクロールバグ修正**:
    - [WebApp] iOS Safari等で `padding-bottom` がスクロール領域として認識されないブラウザ不具合を回避するため、ピッカーの余白を `::before` および `::after` 擬似要素による実体ブロックに置換。これにより、項目数の少ないリストでも確実に末尾までスクロール・選択可能に。

## v3.7.2 (2026-03-02)
- **スタートアップUIのパディングバグ修正**:
    - [WebApp] 「種目選択」など選択肢が5個未満のリストにおいて、ダミー余白を自動追加するJSロジックを追加し、一番下の項目が選択カーソル（中央）までスクロールできるよう修正（※v3.7.3にてCSSベースの完全修正へ移行）。

## v3.7.1 (2026-03-02)
- **UIフィードバックの修正**:
    - [WebApp] スタートアップ画面のドラムロール式ピッカーの構造をCSS標準機能（230px高・46px項目・92px上下パディング）に完全リビルドし、選択カーソルがどれだけスクロールしても絶対にズレないよう数学的に完全解決。

## v3.7.0 (2026-03-02)
- **UIフィードバックと操作性の大幅な改善**:
    - [WebApp] 音声フィードバックエンジン（ToneGenerator）をフルスクラッチで再構築。単音のビープ音から、Web Audio APIを使った複数波形合成（FM音源風）とADSRエンベロープによる高級感のあるサウンド（BELL、GAME、BEEP、MECHANICAL）へ進化。
    - [WebApp] スタートアップ画面のドラムロール式ピッカーについて、表示項目が少ない場合（種目など）に下までスクロールできないバグを解消。
    - [WebApp] ピッカーの選択ロジックをピクセル割り算から絶対座標判定へと変更し、長くスクロールしても絶対に表示枠と選択カーソルがズレないように完全修正。
    - [WebApp] 「SET START」を押した瞬間に、ピッカーが必ず「スクワット・60kg・10回」の初期位置に強制スナップするよう初期化ルーチンを修正。
    - [WebApp] セットのリセット時や中断時に「内部のCSV用生データログ (`rawLogBuffer`)」がクリアされず、次のセットのエクスポートデータに過去のデータが混入し続けるメモリリークバグを修正。

## v3.6.1 (2026-02-28)
- **異常検出耐性の向上**:
    - [WebApp] スマートフォンケース等にデバイスを入れた際に生じる速度減衰によって、スクワットが「遅すぎてノイズ」と判断されてしまう問題（足切り）を修正。
    - スクワットの最小認識ピーク速度（minPeak）を `0.25m/s` から `0.10m/s` へ大幅に引き下げ。デッドリフトとベンチプレスもそれぞれ `0.15m/s` へ引き下げ、ゆっくりとした高重量挙上でも確実にカウントされるように改善。
    - [Firmware] ケース内でデバイスが激しく揺れた際、回転安全装置（ストッパー）が誤作動して速度が0になる問題を解消するため、ジャイロの許容上限を `300dps` から `800dps` へ緩和。
    - [Firmware] デバイスがポケットやケース内で「逆さま（Upside Down）」に配置された際、鉛直加速度がマイナスとして算出され、バウンド防止フィルター（非対称減衰）によって速度が即座に0にされていた深刻なバグを修正。どのような配置でも絶対的な挙上速度を正しく積分するように改良。

## v3.6.0 (2026-02-28)
- **システムの堅牢性（ロバスト性）向上とバグ修正**:
    - [Firmware] 異常な回転（ジャイロ300dps以上）を検知した際、速度の強烈な減衰（0.80）をかけ、異常値が数フレーム残る現象を防ぐように修正。
    - [WebApp] Bluetooth接続が途切れた際に気付けるよう、`gattserverdisconnected` イベントをリッスンし、UI（接続ボタン周りのバッジ）を赤くして「切断されました」と表示する機能を追加。
    - [WebApp] センサーからごく微小なノイズ（0.10m/s未満）が送られ続けた場合、セット自動終了タイマー（30秒）がリセットされてしまうロジックを修正。
    - [WebApp] 通信障害等により文字化けデータ（NaN）が飛んできた場合、アプリがクラッシュ・フリーズしないように「直前の正常なVelocity値」でフォールバックしてしのぐ処理を追加。

## v3.6.2 (2026-02-28)
- **ケース特有の「滑らかな動作」に対する計測漏れを修正（ZUPTバブ）**:
    - [Firmware] スマホケース等に入れてデバイスの振動（ノイズ）が完全に失われた際、ゆったりとしたスクワットの初動加速度が「静止判定（±0.05G）」の枠内に収まり続けてしまい、速度が100%カットされるバグを修正。
    - 静止判定の加速度閾値を `0.95〜1.05G` から `0.97〜1.03G` へとより敏感に狭めたことで、ケースに入れた状態のブレのないスムーズな挙上でも確実に加速を拾うようになりました。

## v3.6.3 (2026-02-28)
- **3Dプリント等「完全固定ケース」におけるZUPT（静止判定）ハングバグを修正**:
    - [Firmware] ケース内でデバイスが「バーベルと完全に一体化（一切ブレない）」した場合、高重量スクワット中のゆっくりした加速度（0.04G等）が「止まっている」と誤認され、速度が0.00m/sに張り付く物理的な限界を修正。
    - ジャイロ（角速度）の静止判定ストッパーを `15.0dps` から `3.0dps` へと極限まで厳格化。人がバーベルを担いでいる間の微小な身体の揺れ（3dps以上）がある時は強制ゼロ化処理（ZUPT）が一切発動しなくなり、重く滑らかなスクワットでも完璧な速度積分が可能になりました。

## v3.5.0 (2026-02-28)
- **バッテリー監視機能の追加**:
    - XIAO nRF52840のハードウェア（内部P0.31ピンの分圧抵抗）を利用し、LiPoバッテリー（3.7V）の電圧を読み取る機能をファームウェアに実装。
    - 標準のBluetooth `Battery Service (0x180F)` をファームウェアに組み込み、残量（%）を定期送信。
    - Webアプリ画面右上にバッテリー残量アイコン（100%〜0%）と色による状態表示（緑/黄/赤）を追加。

## v3.4.5 (2026-02-28)
- **ZUPT（静止判定）の永続ロックバグ修正**:
    - デバイスを傾けたまま静かに置いた際、重力が1Gではなくなる（センサー座標の上下から外れる）のに静止判定が働き続け、Madgwickフィルターが実際の重力方向を見失ったまま「速度0.00m/sにスタック（回復不能）」する致命的なバグを修正。
    - 静止判定の条件に `acc_near_1g` (重力が1Gとほぼ等しい場合のみ) を追加。
    - 姿勢がスタックした場合に備え、長時間静止イベント（100フレーム）が続いた際にジャイロ成分を0として強制的に重力方向を再学習（リセット）させるフェイルセーフを追加。

## v3.4.4 (2026-02-28)
- **ドリフト制御アルゴリズムの抜本的な改善**:
    - ロックアウト直後の微細な震えやバウンドによる「速度のリング（余韻）」をピタッっと0に落とすための「非対称(Asymmetric)減衰アルゴリズム」をファームウェアに導入。
    - 挙上終了後、速度が低下している時は0へ強烈に吸い付かせるソフトランディング処理を追加。
    - 静止判定（ZUPT）のジャイロ閾値を緩和し、判定時間を短縮。より素早く「静止」を捉えるように変更。

## v3.4.3 (2026-02-25)
- **ハードウェア配線の更新**:
    - 通信安定化のため、VDDIO、~CSを3V3へ、AD0をGNDへ直接接続する配線（ハードワイヤリング方式）を標準とし、ドキュメントおよびファームウェア制御を変更。

## v3.4.2 (2026-02-25)
- **ワークフローの改善**:
    - アプリ起動時の初期画面を「SET START」待機状態に変更し、即座にメインUIを確認できるよう改善。
    - セット開始（START押下後）に5秒間のカウントダウン（音声付き）を追加し、安全な準備時間を確保するよう改善。

## v3.4.1 (2026-02-25)
- **バグ修正**:
    - モーダル画面のピッカー（種目、重量、目標回数）で、選択している要素とハイライト表示がズレる不具合を修正。

## v3.4.0 (2026-02-25)
- **UIレイアウトの大幅な刷新と縦幅のスリム化**:
    - リアルタイム・ピーク速度を画面上部に大きく配置し、視認性を劇的に向上。
    - 各種情報（目標回数、直近ピーク、疲労度）をその下にコンパクトにまとめ、不要な余白を削減。
    - コントロールパネルの「SET START」ボタンを画面下部に大きく配置。
- **ワークフローの完全自動化（自動記録・自動終了）**:
    - 目標回数 (Reps) を開始時に設定できるよう第3のピッカーを追加（1〜30回）。
    - SET STARTと同時にカウントダウン無しで自動的に波形記録を開始。
    - レップを行うたびに自動判定し、目標回数到達、または疲労度限界に達した時点で自動的にセット終了と記録完了が行われるよう進化。

## v3.3.1 (2026-02-20)
- **スタートアップUIの不具合修正**:
    - 重量ピッカーの選択肢が重複して表示される問題を解消。
    - PC表示時のレイアウト崩れを修正し、画面中央に正しく配置されるよう調整。
    - 各種ブラウザでのスクロールバー非表示設定を強化。

## v3.3.0 (2026-02-20)
- **スタートアップUIの刷新**:
    - サイトを開いた際に、種目と重量を直感的に選択できるiOSスタイルの「ドラムロール型ピッカー」を導入。
    - 半透明の美しいオーバーレイUIを採用し、トレーニング開始前のセットアップをスムーズに。
    - 重量選択は0kgから300kgまで2.5kg刻みでスクロール選択可能。

## v3.2.9 (2026-02-20)
- **バウンス・ロックアウト干渉の解決**:
    - バウンス（反動）として弾かれたノイズが、次のレップの不感時間を上書きして開始を遅らせてしまう問題を解決。ノイズではタイマーがリセットされないようにしました。
    - バウンスフィルターの適用開始を「1レップ目終了後」からに早め、より初期からノイズを除去。
    - 不感時間を1.0秒にバランス調整。

## v3.2.8 (2026-02-20)
- **バウンスノイズ対策**: 
    - 不感時間を400ms→1200msに再調整。挙上後の反動がレップとして誤カウントされる問題を解決。
    - ベスト速度の40%以下のピークを「バウンスノイズ」として自動排除するフィルターを追加。
    - ログに `⚠️ Bounce filtered` と表示され、排除理由が確認可能。

## v3.2.7 (2026-02-20)
- **【致命的バグ修正】レップ判定ロジックの完全書き直し**: 
    - **閉じ括弧の欠落（構文エラー）**: v3.2.6の修正で導入されたブラケットの不整合により、レップ判定コード全体がJavaScriptパースエラーで動作していなかった可能性を修正。
    - **速度ギャップ問題の解消**: 速度がEND_THRESHOLD(0.05)とSTART_THRESHOLD(0.12)の間にある時、終了カウンターがリセットされず判定がスタックする問題を修正。新たに「中間ゾーン」ブランチを追加し、この範囲では「まだ動いている」と正しく判定。
    - 不感時間を1500ms→400msに短縮。終了閾値を0.05に引き下げ。
    - デバッグログに、レップ確定時の持続時間と、却下時の理由を絵文字付きで表示。

## v3.2.6 (2026-02-20)
- **レップ判定の安定性を大幅向上**: 
    - セット間の「自動リセット（静止判定）」時間を5秒から30秒へ大幅に延長。100kgスクワット等の高重量トレーニングで、次のレップへのセットアップ中にデータが消える問題を解決。
    - レップ開始閾値を0.15→0.12に、終了閾値を0.12→0.10に引き下げ、より微細な挙上動作も確実にキャッチ。
    - SQ（スクワット）の最小ピーク閾値を0.35→0.25に調整し、粘るようなスローな挙上でもカウントされるよう改善。
    - ログ機能に「レップ確定」および「無効なレップの理由」を表示するデバッグ機能を追加。
- **グラフ表示の最適化**: 
    - 波形の横幅を拡大（表示時間を12秒→8秒に短縮）し、挙上の動きをより詳細に観察可能に改善。
    - グラフの底辺を0m/sに固定。マイナス方向の表示を廃止し、挙上速度の視認性を向上。
    - 縦軸のスケールを3.0m/sに調整し、主要なVBT種目で最適なスケール感を実現。

## v3.2.5 (2026-02-20)
- **レップ判定ロジックの修正**: アプリ側 (`index.html`) で挙上時間 (`duration`) の定義が漏れていたバグを修正。これにより時間ベースのノイズフィルタリングが正しく機能するように復旧。
- **データ分析による動作確認**: 提供された100kgスクワットのデータに基づき、不感時間（ロックアウト）と静止時の速度減衰（Soft ZUPT）が正常に機能していることを確認。

## v3.2.4 (2026-02-20)
- **レップ検出バグの修正**: レップ終了時に不感時間の計算ベース（lastRepTime）が更新されていなかった重大なミスを修正。これにより「1.5秒の不感時間」が正しく機能するようになり、挙上直後の揺れによる誤検知を完全にシャットアウト。
- **ファームウェアの残留速度除去能力を2.5倍に強化**: 静止時の速度減衰係数を0.998→0.995に変更。激しい動きの後の速度収束を劇的に早めました。
- **超高重量対応**: 加速度ノイズしきい値を0.06Gに引き上げ、担いで立っている際の細かな筋肉の震え（シバリング）を無視するように調整。

## v3.2.3 (2026-02-20)
- **「えぐいドリフト」への徹底対策**: 
    - 静止判定（ZUPT）の加速度閾値を0.1G→0.25Gに緩和。高重量担ぎ時の微振動でも静止とみなせるように改善。
    - 速度積分に「自然減衰（リーキー積分）」を導入。動作していない時の微小な計算誤差が蓄積するのを物理的に防ぐ。
    - プログラム内の経過時間（dt）計算のガード値を調整。通信遅延による計算飛びを抑制。
- **ノイズ除去強化**: 加速度のデッドゾーンを広げ、震えによる速度増加をシャットアウト。

## v3.2.2 (2026-02-20)
- **挙上後の誤検知対策を強化**: 挙上直後の「揺れ・弾み」を拾わないよう不感時間（LOCKOUT_MS）を250ms→1200msに大幅延長。
- **ノイズフィルタリング改善**: 最小継続時間（MIN_DURATION_MS）を300ms→350msに調整し、単発の衝撃音的なノイズを排除。
